import 'package:flutter/material.dart';

class CalculatorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('투자 계산기'),
      ),
      body: Center(
        child: Text('투자 계산기 페이지입니다.'),
      ),
    );
  }
}
